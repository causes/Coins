<h1>Editing User</h1>

<% form_for @user, :url => url_for(
  :controller => "admin/users", 
  :action => "update",
  :id => @user.id) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Update" %>
<% end %>
